import { ServerPathPipe } from './server-path.pipe';

describe('ServerPathPipe', () => {
  it('create an instance', () => {
    const pipe = new ServerPathPipe();
    expect(pipe).toBeTruthy();
  });
});
